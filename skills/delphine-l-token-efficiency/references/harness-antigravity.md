# Antigravity (`agy`) routing
- Skills in `.agents/skills/` or `~/.gemini/config/skills/`
- No nested subagent files; load role skills in the main agent
- Prefer sequential skill activation over pretending to spawn
