# Grok Build model routing
- Default session: grok-4.6
- Quality Auditor: same, or the highest-capability Grok model the spawn API accepts
- Explore/plan: built-in `explore` and `plan` types (read-only)
- Depth 1: a subagent cannot spawn; directors must be the primary session
