# Gemini CLI model routing
- Default / implementation: Flash-class
- Auditor / architecture: Pro-class
- Subagents: `.gemini/agents/*.md`, invoke `@name`; children cannot spawn
- Skills: `activate_skill` after description match
